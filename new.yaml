#Create eg: switch.yaml file if not created yet-Editable yaml file name

#Display switch.yaml or custom file for edit.Load default settings that can be edit from example

#write files to configuration.yaml

#Check configuration.yaml

